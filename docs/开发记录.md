# 开发记录

> 本文件为**追加式**开发日志：每次改动在顶部新增一节，历史记录永不覆盖。
> 关联文档：`docs/Agent化升级计划.md`（Agent 化升级计划）、`docs/技能使用规范.md`（前端技能路由规范）、`AI模块架构文档.md`（AI 模块设计）

---

## 2026-09-20（八）— P3 选课轮次 + 选课/补退选（六道校验链）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 迁移：`selection_round` / `selection_round_scope` / `course_selection.round_id` + 三种状态种子 | ✅ |
| 2 | `SelectionRoundService`：轮次 CRUD、开关、适用范围、学生侧状态判定 | ✅ |
| 3 | **改造** `CourseSelectionService`：六道校验链 + 退课窗口校验 | ✅ |
| 4 | 新查询：学生时间冲突（`class_time` 自连接）、学期已选学分、已通过 course_code | ✅ |
| 5 | `SelectionRoundController` + `CourseSelectionController` 扩展 + 权限登记 | ✅ |
| 6 | 单元测试 23 项（后端合计 **112 项全绿**） | ✅ |
| 7 | `.dsh/verify-p3-api.ps1`：**106 项**接口断言，六道校验逐条覆盖 | ✅ |
| 8 | `edujwxt.sql` 同步 P3 表与种子；新装库校验加 P3 断言 | ✅ |
| 9 | 前端：选课轮次管理页（管理员）+ 选课页（学生） | ✅ |
| 10 | `.dsh/verify-p3-ui.cjs`：**43 项**浏览器断言（三态渲染 + 真实选/退往返） | ✅ |

### 用户明确的两条规则，落到代码里是这样

> 只有管理员**开启选课**后学生才能选；否则**只能看**
> 选课持续期间**可以退**，否则要等**补退选**期间才能退

所以"能不能选"是三个条件的合取：`status=1`（管理员开了开关）
∧ 学生在轮次适用范围内 ∧ 当前时间落在选课窗口内。少判任何一个都会放行不该放行的选课。
"能不能退"则是 `status=1` ∧（在选课窗口内 **或** 在补退选窗口内）。

**三个状态都做了种子数据**，时间窗用 `NOW()` 相对计算（写死日期的话过一阵子演示数据就全过期，
新装库的人会看到"所有轮次都已结束"，以为功能坏了）：

| 种子轮次 | 学期 | 状态 | 意图 |
|---|---|---|---|
| 第一轮选课 | 2024-2025-1 | status=1，选课窗口内 | 可选可退 |
| 补退选 | 2024-2025-2 | status=1，选课窗口已过、退课窗口内 | **只能退** |
| 第一轮选课（未开启） | 2025-2026-1 | status=0，窗口有效 | 只能看 |

### 六道校验链（只有一处实现）

```
1 轮次开启 → 2 适用范围 → 3 学分上限 → 4 未修过(同 course_code) → 5 无时间冲突 → 6 容量未满
```

两个刻意的设计：

- **校验方法返回"原因"而不是抛异常**（`selectionBlocker`）：这样"可选课程列表"能把原因
  显示在按钮旁边，`select()` 再把同一份原因转成业务异常。否则就会出现
  "列表说能选、提交却说不能"——两套判断迟早会不一致。
- **学生维度的冲突是 P2 之外新增的第三种冲突来源**（P2 只有教师与教室）。
  判据**完全复用**闭区间重叠那一条，只是参与方从"教师/教室"换成"该生已选课的排课"，
  用 `class_time` 自连接一次查完。冲突语义全项目仍然只有一个定义。
  ⚠️ **候选课程没有排课时查不出冲突**，这是正确行为：没排课的课不可能和别人撞。

### 【假设】补退选窗口只用于退选

用户原话是"否则要等**补退选期间才能退**"，只提到退；且本表字段名是
`drop_start/drop_end`（而不是 `adjust_*`）。因此实现为**补退选期间只能退、不能选**，
并在设计文档的假设汇总里登记（编号 9）。若实际需要"补选"，改
`SelectionRoundServiceImpl#evaluate` 里 `canSelect` 的判定即可（一行）。
**这条需要用户确认。**

### 前端（P3，2 个页面）
`/selection-round`（管理员：轮次表格 + 新建/编辑 + 开关 + 适用范围抽屉）、
`/course-selection`（学生：状态横幅 + 可选课程表 + 我的已选课程）。

**三态判定前端也要有一份**：管理员侧后端没有返回"当前是否在时间窗内"，
所以表格里的 `开启中·可选 / 补退选·只能退 / 已关闭·只能看` 是**前端按当前时间实时算的**，
页面上明确写了这一点，免得管理员以为是后端给的数字。
另外前端多分了一个 `已开启·不在时间窗内` 状态——把"开关开着但时间窗都没到"硬塞进
"可选"或"只能退"里，页面就在骗人。

学生侧的横幅则**完全以服务端为准**（`canSelect`/`canDrop` + `reason`），前端不重算：
判定要综合开关、时间窗、适用范围、学分上限，前端再算一遍必然走偏。

**被挡住的课不是"能点然后报错"，而是按钮禁用 + 旁边写明原因**（原因就是服务端给的那句话）。

### 本轮发现的问题

1. **`SelectionStatus.readOnly()` 是又一个"派生方法不序列化"的坑**
   我在 record 上写了 `readOnly()` 想给前端用，但 **Jackson 只序列化 record 组件**，
   接口报文里根本没有这个字段——正是 P1 `AuditResult.satisfied()` 那个坑的翻版，
   而且这次是**我在已经踩过一次之后又踩了一次**。
   好在接口校验脚本第一时间把它照出来了（断言的 `$r.data.readOnly -eq $true` 直接失败）。
   已删除该派生方法，并在方法注释里写明"需要只读请在调用方用 `!canSelect && !canDrop` 推导"，
   测试里也加了一个 `readOnly(status)` 私有 helper 来承担这件事。
2. **把这条规则的边界搞清楚了（这次实测过，不是推测）**：
   | 形态 | 会不会进 JSON |
   |---|---|
   | **record** 的派生方法（如 `satisfied()`、`readOnly()`） | **不会**（Jackson 只认 record 组件） |
   | **POJO** 的 `isXxx()` / `getXxx()`（如 `SelectionRoundScope.isUnrestricted()`） | **会**（当属性 `unrestricted` 序列化） |
   | 任何带参数的方法（如 `matches(...)`） | 不会 |
   > 已用真实报文验证：`GET /selection-round/3/scope` 返回里确实有 `"unrestricted":false`。
   > 所以**"派生方法不序列化"只对 record 成立**，POJO 上的 `isXxx()` 是正常序列化的。
   > 我一度照搬了子代理"都不是序列化"的说法，实测后纠正——**这类结论必须自己验一遍**。
3. **冲突提示只给了课程名，没给课程代码**
   同一门课可能有多个教学班、名称也可能重复，改成 `《课程名》(课程代码)`，
   学生才能定位到具体哪一门（接口断言专门校验了代码出现）。
4. **种子数据根本测不到第 4/5/6 道校验**
   - 全库**不存在**"已通过但没选过"的课程（第 4 道无从触发）
   - 种子里三门有课表的课分别排在周一/周二/周三，**两两不重叠**（第 5 道无从触发）
   - 全库**没有满员课程**（第 6 道无从触发）
   所以第 4/5/6 道放在接口校验脚本里，用可清理的 fixture 覆盖（造一个"同一课程代码的另一个教学班"
   触发第 4 道、给两门课排重叠的课表触发第 5 道、造一门容量 1 的课并让另一学生选满触发第 6 道）。
   单元测试只覆盖**不写库就能验证**的部分，类注释里写明了这个分工。
5. **又是"Promise reject 未接住"这一类缺陷（这次在前端）**
   `ElMessageBox.confirm` 在用户**取消**时是 **reject**，不是返回 false；
   4 处（开关轮次、删除轮次、删除范围、退课）都会因此抛未处理的 Promise 异常，
   已全部 `try/catch`。这和 P1 的 `formRef.validate()` 是**同一个缺陷类别**，已在下方单列。

### 缺陷类别清单（跨阶段反复出现，值得单独记）
| 类别 | 表现 | 已出现的实例 |
|---|---|---|
| **Promise reject 未接住** | 用户取消/校验失败时抛未处理异常 | P1 `validate()`、P3 `ElMessageBox.confirm` ×4 |
| **派生方法不进报文** | 前端读到 `undefined`，分支永远走不到 | P1 `AuditResult.satisfied()`、P3 `SelectionStatus.readOnly()` |
| **权限规则顺序** | 具体路径必须排在 `/**` 之前，否则角色被错拒/错放 | P1 `/gpa/my`、P2 `/class-time/apply/my`、P3 `/selection-round/current` |
| **断言不会失败** | 断言写得太弱，漏洞照样通过 | P1 只检查列宽、P3 `>= 0` 恒真（自查时删掉） |
| **种子数据测不到边界** | 用例看着写了，其实触发不到那条分支 | P3 第 4/5/6 道校验 |

### 新踩的坑（PowerShell 又一次）

**变量名大小写不敏感**：`$STU2 = '2023002'` 与 `$stu2 = Login ...` 是**同一个变量**！
后者把 token 写进了前者，于是断言里 `studentId=$STU2` 实际发的是**一整串 JWT**，
后端查不到学生 → 返回"学生不存在" → 断言失败，而排查方向完全跑偏（我一度怀疑是权限或学期作用域问题）。
已改名为 `$ID1/$ID2` 并在脚本里写了注释。
> 这是 PowerShell 的第 4 个坑，前三个：函数返回数组被展开导致 `.Count` 失效、
> 逗号比 `+` 结合更紧、正则批量改脚本把括号配平改坏。**校验脚本写完必须看失败详情，不能只看结论。**

### 验证结果（全绿）
- `mvn -o -B test -pl edu-api -am`：**112 项**（新增 23 项）
- `.dsh/verify-p3-api.ps1`：**PASS=106, FAIL=0**，覆盖：
  - 权限：匿名 401（5 条）、学生越权 6 条、**教师无任何选课功能 4 条**、管理员可代查学生
  - 三种轮次状态的 `roundOpen/canSelect/canDrop/maxCredits/reason`
  - **六道校验逐条**：轮次未开启、范围外、超学分上限、已通过同课程代码、
    与已选课时间冲突（并点名冲突课程+周次节次）、容量已满
  - **补退选规则**：选课窗口关闭后不能选、只能退；退课成功后记录消失；
    退一门没选过的课被拒
  - 范围的增删生效（加一条年级 2029 的范围后学生立刻不能选，删掉又恢复）
  - 轮次管理校验：窗口倒置/缺时间/同学期重名/非法状态值/未知轮次
  - 冲突判据的**相邻不算冲突**（把 B 的节次改成与 A 相邻后可选）
- `.dsh/verify-p3-ui.cjs`：**PASS=43, FAIL=0**（真实 Chromium，**全程无未捕获异常**），走通：
  三种轮次状态在管理页渲染成三个**不同**标签 → 学生页横幅三态（成功/警告/信息三色）
  → 补退选期间所有「选课」按钮实测禁用 → **真实选课（学分 19.5→24.5）再退课（→19.5）**
  → 教师菜单既无选课也无选课轮次、越权跳转被拦
- 回归：P1 读 31、P1 写 63、P2 136 —— **全部无回归**
- `npm run type-check` 通过；`eslint` 仅剩既存基线；`npm run build-only` 构建通过
- 新装库校验：新增 `selection_round`/`selection_round_scope` 表与
  "当前可选的轮次恰好 1 个、可退的恰好 1 个、无窗口倒置"等断言

### 累计断言数
后端单测 112 + 接口 31/63/136/106 + 浏览器 67/60/43 = **618 项**

### 下一步
**P4：考试安排**（`exam_schedule`：`exam_type` 期末/补考/期中、考试时间与时长、
考场教室、座位号段、监考教师；`/exam/my` 给学生看自己的考试）。P2 的 `room` 表直接复用。

---

## 2026-09-20（七）— P2 排课 + 教室 800 间 + 冲突检测（含 P1 遗留修复）


| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 迁移脚本：`room`(800)/`class_time`/`course_apply`/`class_time_apply`，幂等 | ✅ |
| 2 | POJO + Mapper：`Room`/`ClassTime`/`CourseApply`/`ClassTimeApply` | ✅ |
| 3 | `ScheduleService`：区间冲突检测 + 教室推荐 + 排课审批硬校验 | ✅ |
| 4 | `CourseApplyService`：提交 / 审批（生成 course）/ 驳回，状态机 | ✅ |
| 5 | `RoomController` / `ClassTimeController` / `CourseApplyController` + 权限登记 | ✅ |
| 6 | **修复 P1 遗留**：`Course.courseCode` 未接入 POJO/Mapper | ✅ |
| 7 | 单元测试 26 项（冲突边界/周次/房间推荐），后端合计 **89 项全绿** | ✅ |
| 8 | `.dsh/verify-p2-api.ps1`：**136 项**接口断言，含完整验收链路 | ✅ |
| 9 | `edujwxt.sql` / `seed_data.sql` 同步 P2 表与 800 间教室 | ✅ |
| 10 | 校验脚本加 P2 表与形状断言，新装库导入通过 | ✅ |
| 11 | 前端：开课申请 / 排课审批 / 教室维护 / 我的课表 | ✅ |
| 12 | `.dsh/verify-p2-ui.cjs`：**60 项**浏览器断言，走通完整业务链路 | ✅ |

### 冲突判据（全项目只此一处）

用户确认的规则：**同一学期 且 星期几相同 且 节次区间重叠 且 周次区间重叠**。
区间重叠用**闭区间**判据 `a.start <= b.end AND a.end >= b.start`：

| 情形 | 结果 |
|---|---|
| 第 1-2 节 vs 第 3-4 节 | **不冲突**（相接不算） |
| 第 3-4 节 vs 第 4-5 节 | **冲突**（第 4 节重叠） |
| 第 1-16 周 vs 第 17-18 周 | 不冲突 |
| 第 1-16 周 vs 第 9-16 周 | 冲突（CS105 就是第 9 周开始的课） |

判据写在 SQL 里（`ClassTimeMapper.xml#selectConflicts`），教室推荐的空闲判定
（`RoomMapper.xml#pickFreeRooms` 的 `not exists`）必须与它**逐字一致**——
写成 `<` 就会变成"相接也算冲突"；两处不一致就会出现"检测说不冲突、推荐却说教室被占"。

**因为判据在 SQL 里，它的测试只能是集成测试**：把 Mapper stub 掉的单测只能验证 Java 分支，
验证不了重叠判据，而重叠判据恰好是这块唯一容易写错的地方。故 `ScheduleServiceTest`
直接打真库、**只读**、不做任何写操作，可重复执行。

### 两个审批流（刻意分开）

用户需求：教师申请开课 → 管理员审批 → 生效；且"教师排课"是单独一步。
故 `course_apply`（开课）与 `class_time_apply`（排课）**各自一张表、各自一条审批流**：

```
教师提交开课申请 → 管理员审批通过 → 生成 course 行（course_code 沿用申请里的）
                → 教师对已通过的课程提交排课申请 → 管理员审批
                → 审批环节硬校验冲突：有冲突则不落 class_time，
                  把冲突详情写进 conflict_info 并报错
```

管理员**没有**"直接插一条课表"的接口，这是刻意的：所有排课都必须留痕在
`class_time_apply` 里（谁申请、谁审批、有没有冲突），否则课表不可追溯。

### 本轮发现并修复的 4 个真实缺陷

1. **提交排课申请时的冲突提示根本没落库**
   `ClassTimeApplyMapper.add` 的 INSERT 少了 `conflict_info` 列，服务里
   `apply.setConflictInfo(...)` 写了个寂寞——提交时明明检出了冲突，教师回头再看申请记录却是空白。
   修复：INSERT 带上 `conflict_info`。（"审批被冲突挡住"那条是通过的，所以只有提交环节的
   提示丢了，属于容易漏掉的那一半。）
2. **自动分配的教室没有回写到申请行**
   审批时若没指定教室，服务会推荐一间并写进 `class_time`，但 `class_time_apply.room_id`
   仍是 NULL。结果：课表说"教1-101"，申请记录说"未分配"——审计链断了。
   修复：`updateReview` 增加 `roomId`（用 `COALESCE` 写，驳回时不抹掉教师期望的教室）。
3. **P1 遗留：`course.course_code` 从未接到 POJO/Mapper**
   P1 建了这一列并用它做培养计划/已修判定的地基，但 `Course` POJO 没有 `courseCode` 字段、
   `CourseMapper.add` 不写它、`CourseMapper.xml` 的 update 也不更新它 →
   **管理员在页面上新建的课程，course_code 永远是 NULL**，判断类功能会静默失配
   （不报错，只是永远匹配不上）。P2 的"审批通过生成 course"正好依赖它，故一并修复，
   并加了 `selectIdByCode` 供排课/选课反查。已在接口层验证（新建课程能带回课程代码）。
4. **`edujwxt.sql` 里放课表种子会写入 NULL 主键**
   `edujwxt.sql` 只建了 CS101/CS102 两门课，其余 10 门在 `seed_data.sql` 里；
   课表种子按 `course_code` 反查 `course.id`，放在 `edujwxt.sql` 里查不到 CS105 →
   `Column 'course_id' cannot be null`，**新装库直接失败**。
   修复：课表种子移到 `seed_data.sql`（那里 12 门课才齐），并用 `EXISTS` 保护。

### 新装的坑（PowerShell，一天内踩了三个）

1. **函数返回数组会被 PowerShell 展开，`.Count` 随之失效**
   `function Arr($v) { return @($v) }` 在只有 1 个元素时返回的是那个元素本身，
   而 **`PSCustomObject` 上的 `.Count` 返回的是"空"（不是 0，也不是 1）**，
   于是断言里拼出 `rows=`，看着像数据是空的，其实数据在。
   修复：计数一律用 `function Cnt($v) { ... return @($v).Count }`（返回真正的 Int32），
   管道/筛选/取下标则用**内联** `@($x)`（内联表达式不经过函数返回，不会被展开）。
   > 这个坑很危险：它让"数据是空的"和"断言写错了"长得一模一样。
2. **逗号比 `+` 结合更紧**
   `@('GET', '/room/free?term=' + $TERM + '&x=1')` 会被解析成 3 个元素
   （`'GET'`、`'/room/free?term='`、`$TERM + '&x=1'`）。必须给拼接加括号。
3. **用正则批量改脚本时括号配平被破坏**
   把 `(Arr $x) |` 替换成 `(@($x) |` 时多出一个左括号，整段脚本直接解析失败。
   教训：批量改完要**立刻跑一遍**，别指望"看起来对"。

### 与既有踩坑清单的呼应
- **含中文的 `.ps1` 一律不碰 PowerShell 读写往返**：`verify-p2-api.ps1` 从一开始就写成
  ASCII-only，教室名用 `[char]0x6559` 拼出来；`validate-edujwxt-sql.ps1` 需要断言
  "每栋楼 100 间"时，改成 `GROUP BY ... HAVING COUNT(*) <> 100` 而不是写中文楼名。
- **权限规则顺序敏感**：P2 新接口同样按"具体路径在前、`/xxx/**` 在后"登记，
  并在接口层实测了教师/学生/匿名三类拒绝。

### 前端（P2，4 个页面）
`/course-apply`（教师：开课申请 + 我的排课申请 + 我的课表）、
`/schedule-approve`（管理员：开课审批 / 排课审批 / 课表总览三个 Tab）、
`/room`（管理员：800 间教室维护，分页筛选）。
排课对话框是重点：时间任一字段变化都**实时重检冲突并列出"和谁撞了"**
（教师冲突与教室占用冲突分组显示），同时给出该时段的空闲教室与「用推荐教室」一键选用；
时间字段 300ms 防抖，卸载时清定时器。

**顺带修掉一个我自己埋的耦合**：`studentMenuList` 原本写成 `[...teacherMenuList]`，
于是"给教师加一个菜单项"会**顺带漏给学生**——学生看到一个点了会被守卫弹回首页的入口。
已抽出共用的 `commonMenuList` 作基座，教师/学生各自在其上追加，仍是三份列表。
（这个 bug 的可怕之处在于：加菜单时看不出问题，只有用学生账号登录才发现。）

### 验证结果（全绿）
- `mvn -o -B test -pl edu-api -am`：**89 项**（含新增 26 项排课测试）
- `.dsh/verify-p2-api.ps1`：**PASS=136, FAIL=0**，覆盖：
  - 匿名 401 / 学生 403 / 教师"可读可申请但不可审批"共 15 项权限边界
  - 教室 800 间、每栋 100 间、容量分档、显示名非空
  - 冲突检测边界：相接不冲突、端点重叠冲突、周次不相交不冲突、跨学期/跨星期不冲突、
    教室维度独立于教师、`excludeCourseId` 语义
  - 教室推荐：跳过被占教室、按容量升序、容量不足时明确报"没有这么大的教室"
  - **完整验收链路**：提交开课申请 → 重复提交被拒 → 管理员审批生成课程（含课程代码）
    → 教师申请排课 → 审批自动分配合适教室 → 课表出现该行
  - 冲突硬拦截：提交时提示、审批时拒绝、不产生课表行、冲突详情落库
  - 同一门课不能和自身重叠（证明审批时**没有**错误地排除整门课）
  - 归属校验：教师不能给别人的课程排课，也不能读别人的申请
- `.dsh/verify-p2-ui.cjs`：**PASS=60, FAIL=0**（真实 Chromium），**全程无未捕获异常**。
  它是**唯一**验证"冲突真的画在屏幕上"的一层，走通：
  教师用表单提交开课申请 → 管理员在审批页点通过（真的生成了课程行）
  → 教师打开排课对话框 → **默认时段就与种子 CS101 撞车，界面列出冲突并点名 CS101**
  → 改成周一 3-4 节后冲突消失（走 success 分支）→ 点「用推荐教室」
  → 提交 → 管理员审批通过 → 课表总览出现该课；
  另含三套菜单互不串味、学生/教师越权跳转被拦、教室页 800 间分页与容量筛选。
- `.dsh/validate-edujwxt-sql.ps1`：新装库导入通过，生成 800 间（8 栋 × 每栋 100 间 /
  100 种楼层房号组合）、3 条课表且 `course_id`/`room_id` 均非空
- `npm run type-check` 通过；`eslint` 仍只有既存的 5 条 `vue/multi-word-component-names`
  基线错误；`npm run build-only` 构建通过

### P2 累计断言数
后端单测 89 + P2 接口 136 + P2 浏览器 60 = **285 项**（P1 另有 63 + 31 + 63 + 67 项未回归）

### 下一步
**P3：选课轮次 + 选课/补退选**（六道校验链：轮次开启 → 适用范围 → 学分上限 →
未修过 → 无时间冲突 → 容量未满）。P2 的 `checkConflict` 会在第 5 步被复用。

---

## 2026-09-20（六）— P1 前端三页 + 写接口/浏览器双层验证（P1 完结）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | `/gpa` 我的绩点：总览卡 + 逐门明细表 + 学分进度卡 | ✅ |
| 2 | `/training-plan` 我的方案：方案信息 + 必修/选修清单 + 学分进度 | ✅ |
| 3 | `/training-plan-manage` 培养计划维护：列表 + 筛选 + 方案表单 + 课程明细抽屉 | ✅ |
| 4 | 取数抽到 `composables/useMyGpa.ts`、`useMyTrainingPlan.ts` | ✅ |
| 5 | `types/models.ts` 补齐 10 个 P1 类型；router/Layout 菜单接入 | ✅ |
| 6 | 新增 `.dsh/verify-p1-write.ps1`：**63 项**写接口 + 越权 + 校验断言 | ✅ |
| 7 | 新增 `.dsh/verify-p1-ui.cjs`：真实 Chromium 驱动，**67 项** UI 断言 | ✅ |
| 8 | **修复 3 个真实缺陷**（见下） | ✅ |

### 目录约定（新增页面）
三个页面都按「**容器 + 单一职责子组件**」拆：`index.vue` 只做取数与组合，
渲染细节落在 `components/` 下，取数逻辑落在 `composables/` 下。
带双向契约的子组件用 `defineModel`（如 `TrainingPlanForm` 的显隐）。

### 路径拆分（避免复用一个路由）
`/training-plan` 是**学生**的「我的方案」，`/training-plan-manage` 是**管理员**的维护页。
最初两者复用同一路径，导致管理员菜单点进去看到学生页——已拆开。

### 本轮发现并修复的 3 个真实缺陷

1. **`audit.satisfied` 恒为 `undefined`，成功分支永远走不到**
   `CreditService.AuditResult` 是 record，`satisfied()` / `creditGap()` 是**派生方法**；
   Jackson 只序列化 record 组件，报文里根本没有这两个字段（已用真实报文核对）。
   `CreditProgressCard` 原来直接读 `audit.satisfied`，于是「毕业学分要求已全部达成」
   这条 `success` 分支**永远不可达**，全达标的学生也会看到"尚未满足"。
   修复：前端由 `planFound && creditSatisfied && requiredSatisfied && electiveSatisfied`
   自行推导（语义与后端 `satisfied()` 一致），并在 `models.ts` 里删掉这两个幻影字段、
   写清原因，避免以后再被误用。
2. **教师菜单里出现了用不了的入口**
   `Layout.vue` 原来教师和学生共用一份 `commonMenuList`（含"我的方案/我的绩点"），
   而这两个路由只放行 `admin/student`——教师点进去会被守卫弹回首页。
   与"**教师没有绩点功能**"的要求直接冲突。
   修复：拆成 `teacherMenuList` / `studentMenuList`（学生版在教师版基础上追加两项）。
3. **表单校验失败产生未处理的 Promise 异常**
   `TrainingPlanForm.submit()` 直接 `await formRef.validate()`；
   Element Plus 的 `validate()` 校验失败时是 **reject** 而不是返回 `false`，
   于是每次管理员空表单点"保存"都会抛一个 unhandled rejection（浏览器记一条 pageerror）。
   修复：按 `Login.vue` 既有写法 `try/catch` 后 `return`（提示由 el-form 自己渲染）。

> 缺陷 1 和 3 都是**接口/类型检查发现不了**的：前者接口 200、类型也"通过"（字段声明为可选），
> 后者功能表现"正常"（错误提示照样显示）。只有把页面跑起来才会暴露——这正是补 UI 验证的理由。

### 新增验证脚本

**`.dsh/verify-p1-write.ps1`（63 项）** —— `verify-p1-api.ps1` 只覆盖读接口，
写接口此前从未真正跑过。本脚本覆盖前端实际调用的全部变更接口：
- 匿名 → 401（4 个变更路由）；学生/教师 → 403（各 4 个）
- 新建校验：缺方案名/年级/专业、纯空白方案名 → 业务错误且**未落库**
- 新建成功返回新 id；同专业年级重复新建 → 被拒
- 列表筛选（专业+年级、仅年级、无匹配）与联表 `majorName`
- 明细校验：空课程代码、非法类别、缺 planId、不存在的 planId
- 明细增删、重复课程代码被拒、学分快照
- 更新：缺 id、改到已被占用的专业+年级（**不静默覆盖**）、持久化核对
- 脚本自带 fixture（年级 `2099`）**运行前后各清一次**，可重复执行

**`.dsh/verify-p1-ui.cjs`（67 项）** —— 真实 Chromium 打开三个页面，
把渲染结果与接口报文**逐项对照**（不是断言固定字符串，改了种子数据也不会假红）：
- 学生 2023001 `/gpa`：GPA 3.8834、计入学分 14.5、通过 4 门、排名 1/2、
  明细按绩点降序、`90.4→4.04` 小数正确、学分进度三行、必修已达标
- 学生 2023001 `/training-plan`：方案信息、必修 4 门全"已通过"、选修 8 门"未修读"、
  `suggestSemester=1` 正确人化为"大一上"
- 学生 2023002（无方案）：两条空态分支的文案与"隐藏课程清单卡"
- 管理员 `/training-plan-manage`：列表、筛选/重置、课程明细抽屉（12 门 + 学分合计）、
  新建对话框三项必填校验、取消不产生数据
- 菜单可见性 + 路由守卫：管理员/学生/教师三套菜单互不串味，越权跳转被拦
- **全达标分支**：种子数据里没有"全部达标"的学生，故用 `page.route` 改写审核标志为全 true，
  断言 `success` 分支与样式可达（缺陷 1 的回归护栏）

### 验证结果（全绿）
- 后端 `mvn -o -B test -pl edu-api -am`：**63 项**（本轮未改 Java，未复跑）
- `.dsh/verify-p1-api.ps1`：**PASS=31**
- `.dsh/verify-p1-write.ps1`：**PASS=63**
- `.dsh/verify-p1-ui.cjs`：**PASS=67**
- `npm run type-check` 通过；`eslint` 仅剩 5 条**既存**基线错误
  （各页面 `index.vue` 的 `vue/multi-word-component-names` 与 `Layout.vue` 同名规则，全仓库一致）
- `npm run build-only` 构建通过

### 新踩的坑（都很花时间，记下来）

1. **再次用 PowerShell 文本往返改含中文的文件 → 整个文件的中文变成乱码**
   `Get-Content -Raw` + `Set-Content` 会按系统 ANSI 代码页读写，`我的绩点` 变成 `鎴戠殑缁╃偣`。
   （这是同类问题的第 4 次。）**铁律：含中文的文件只能用 write/edit 工具改，
   绝不走 PowerShell 的读写往返。** 本次靠重写整个文件恢复。
2. **Playwright 浏览器版本对不上**：本机装的是 `chromium-1223`，
   而项目 `playwright-core` 钉的是 `1217`，直接启动会报 "Executable doesn't exist"。
   解决：脚本里 `resolveChromium()` 自己找最新已安装的 `chromium-*`，
   用 `executablePath` 指过去，**不下载任何东西**。
3. **API token 与浏览器 session 会互相踢掉**：
   拦截器用 Redis `token:<username>` 做单点校验，**同一个人再登录一次，旧 token 立刻失效**。
   所以校验脚本里"另开一个 fetch 登录拿 token"会让浏览器里的会话开始 401。
   解决：一律从 `sessionStorage` 取浏览器正在用的那个 token（`pageToken()`）。
4. **hover 触发的下拉框会挡住点击**：顶栏用户菜单是 `el-dropdown` 默认 hover 触发；
   关掉右侧抽屉后鼠标正停在它上面，浮层展开后**拦截了对卡片头部按钮的点击**
   （Playwright 报 "subtree intercepts pointer events"）。解决：点击前把鼠标挪到空白处并按 Esc。
5. **浏览器端异常在 Windows 控制台里被打成 `Object`**：`[object Object]` 过一遍 PowerShell 就面目全非。
   解决：把 `pageerror` / `console.error` 原样写进 `.dsh/verify-p1-ui.diag.json` 再看文件
   （该文件已加入 `.gitignore`）。
6. **沙箱与 Vite/Playwright**：`npm run dev` / `build-only`（Vite 配置加载会 `child_process.exec`）
   与 Playwright 启动 Chromium（`--remote-debugging-pipe` 走命名管道）都会被
   workspace-write 沙箱以 `spawn EPERM` 拒绝，需一次性提权执行。

### 已知小问题（未修，影响可忽略）
- 路由 `/gpa`、`/training-plan` 允许 `admin`，但管理员没有对应菜单入口；
  若手工输入 URL，`/gpa/my` 会因"学号非数字"返回业务错误，页面显示加载失败。
  菜单不可达，暂不处理。

### 下一步
**P2：排课 + 教室（约 800 间）+ 冲突检测**（同一学期 + 同一星期 + 节次重叠 + 周次重叠）。

---

## 2026-09-20（五）— P1 接口层：绩点/排名/培养计划 REST + 权限登记（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | `GpaService.rankInMajor`：专业内绩点排名（只返回本人名次） | ✅ |
| 2 | `GpaController`：学生本人 3 个接口 + 管理员 3 个接口 | ✅ |
| 3 | `TrainingPlanController`：学生 `/my` + 管理员 CRUD | ✅ |
| 4 | `TrainingPlanService` 补管理员 CRUD（新建查重、明细增删） | ✅ |
| 5 | `LoginInterceptor` 登记新接口权限 | ✅ |
| 6 | 新增 `.dsh/verify-p1-api.ps1`：31 项接口与越权断言 | ✅ |

### 接口清单与权限

| 接口 | 角色 | 说明 |
|---|---|---|
| `GET /gpa/my` | 学生 | 本人绩点 + 排名（合并返回，前端一页两用） |
| `GET /gpa/rank` | 学生 | 本人专业内排名 |
| `GET /gpa/audit/my` | 学生 | 本人毕业学分审核 |
| `GET /gpa/{studentId}`、`/gpa/rank/{studentId}`、`/gpa/audit/{studentId}` | 管理员 | 查指定学生 |
| `GET /training-plan/my` | 学生 | 本人适用方案 + 课程明细 + 审核结果 |
| `GET /training-plan`、`/training-plan/{id}` | 管理员 | 方案列表/详情 |
| `POST/PUT /training-plan`、`POST/DELETE /training-plan/course` | 管理员 | 方案与明细维护 |

**教师无绩点/培养计划权限**（用户明确要求），已实测被拒。

### 权限实现要点（顺序敏感，容易写错）
`LoginInterceptor` 的规则是**自上而下命中第一条即生效**，因此：
- `/gpa/my`、`/gpa/rank`、`/gpa/audit/my` 必须排在 `/gpa/**`（仅 admin）**之前**，
  否则学生会被后者拒掉
- 用 `/gpa/audit/*`（单层通配）匹配管理员查指定学生，从而**不吞掉**学生的 `/gpa/audit/my`
- 培养计划单独一张 `TRAINING_PLAN_RULES`（`/training-plan/my` 放行学生 →
  `/training-plan/**` 仅 admin），并在 `preHandle` 中按 `RULES → TRAINING_PLAN_RULES` 顺序匹配

### 排名范围
按「**专业 + 年级**」划分（不同年级课程不同，混排无意义），依据累计平均学分绩点降序。
**只返回本人名次与总人数**，不返回他人明细（用户明确要求）。
逐个同学计算绩点时若某人数据异常，按 0 计入并告警，不让整个排名失败。

### 验证结果
- 后端 `mvn -o -B test -pl edu-api -am`：**63 项全绿**（连跑两次一致）✅
- 接口与权限 `.dsh/verify-p1-api.ps1`：**PASS=31, FAIL=0** ✅
  - 学生 8 项正常访问；学生被拒 5 个管理员接口（403）
  - **教师被拒 6 个**绩点/培养计划接口（403）
  - 管理员 6 项正常访问；方案详情含 12 条课程明细
  - 学生数据按 token 隔离（`/gpa/my` 不接受他人 id）
- **真实数据核对**（2023001）：GPA `3.8834`、计入学分 `14.5`、排名 `1/2`；
  逐门绩点 CS101 88→3.8、CS102 90→4.0、CS103 90.4→4.04、CS104 87→3.7，
  **手工验算 Σ(绩点×学分)/Σ学分 = 56.31/14.5 = 3.8834 与接口完全一致** ✅
- 毕业审核：必修全过、总学分 14.5/40（缺口 25.5）、8 门选修待修 ✅

### ⚠️ P1 剩余
前端三个页面（培养计划维护、我的方案、我的绩点）。Agent 新工具按原计划在 P5。

---

## 2026-09-20（四）— P1 实施：培养计划 + 绩点/学分审核（后端核心完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 迁移脚本：3 张新表 + 5 处现有表改造 + 课程代码回填 + 示例方案 | ✅ |
| 2 | `GpaService`：绩点换算与平均学分绩点（BigDecimal） | ✅ |
| 3 | `CreditService`：已修学分与毕业审核（必修逐门 + 选修学分 + 总学分） | ✅ |
| 4 | `TrainingPlanService`：按「专业 + 年级」定位适用方案 | ✅ |
| 5 | `ScoreServiceImpl` 改造：维护 `passed` 派生字段、成绩精度 3 位 | ✅ |
| 6 | 测试 41 → **63 项**（含用户给的 13 条绩点例子逐条断言） | ✅ |
| 7 | **顺带修复**：`edujwxt.sql` / `seed_data.sql` 无列名 INSERT（新装库原本会崩） | ✅ |

### 一、数据库变更（迁移脚本可重复执行，已验证）

新增三张表 `training_plan` / `plan_course` / `gpa_rule`；
`course` 加 `course_code`（**刻意不加唯一约束**：同一门课多学期开课共用 code）；
`score` 加 `makeup_score`（补考）与 `passed`（是否通过），精度 `(5,1)→(6,3)`；
`student` 加 `status`（学籍状态，本轮只加字段）。

回填现有 12 门课程的代码（CS101~CS107、EN101/EN102、MA101、EC101、MG101），
并种子一条示例培养计划（计科 2023 级：必修 4 门 14.5 学分、选修要求 25.5、总学分 40）。

### 二、绩点公式——设计阶段连续理解错两次，最终以此为准

```
若 分数 < 60:  绩点 = 0
否则:          绩点 = 分数 / 10 - 5
```

**用户给的 13 条例子全部通过**（60→1.0、60.1→1.01、62.3→1.23、65→1.5、65.333→1.5333、
75→2.5、75.76→2.576、85→3.5、87.365→3.7365、95→4.5、100→5.0、59.9→0、0→0），
`GpaServiceTest` 逐条断言，其中一条专门防 double 精度漂移（87.365 必须精确等于 3.7365）。

规则集中实现于 `GpaServiceImpl`：**「是否通过」也由它判定**，`CreditService` 与
`ScoreServiceImpl` 都调用它，避免"绩点算通过了、passed 没标记"的不一致。

### 三、毕业审核逻辑
```
定位方案（专业+入学年级）→ 必修逐门比对 → 选修学分求和 → 总学分比对
```
- 未通过必修区分 `NOT_TAKEN`（要选课）与 `FAILED`（要补考），两者用户动作不同
- 无适用方案时返回 `planFound=false` 而不报错，让上层能区分"没方案"与"不达标"
- 同一课程代码只计一次学分；已修学分取自**实际修读课程**的学分

### 四、过程中发现并修复的严重问题（与非代码文件有关）

**`edujwxt.sql` 与 `seed_data.sql` 的无列名 INSERT 会导致新装库崩溃。**
两个文件共 115 条 `INSERT INTO t VALUES (...)`，本次给三张表加了列后全部值错位。
后果：`docker compose up` 的首次建库（initdb 01/02）会报
`Column count doesn't match value count` 或**静默写入错位数据**。

修复：把所有 INSERT 改为带列名（覆盖两个文件所有表，为将来加列做好防护），
并补齐三张表缺失的新列值（course_code 按课程名映射回填）。
新增验证脚本 `.dsh/validate-edujwxt-sql.ps1`：把两个文件改名后导入**临时库**，
断言导入无错 + 行数 + 关键列完整性，再删除临时库。

（该脚本不得不改写表名：这两个文件含 `DROP TABLE` 且没有 `USE`，直接对着真实库跑会清空数据。）

### 五、这段工作里我反复犯的两个错误（值得记录）

1. **`.ps1` 里写中文，连续踩三次**。Windows PowerShell 5.1 按 ANSI 读取 `.ps1`，
   中文被破坏导致语法错误。第三次才彻底解决：把中文外置到 UTF-8 的
   `.dsh/course-code-map.json`，脚本保持纯 ASCII。
   **这条早已写在本文档 §8.4 第 1 条**，是"知道却没做到"。
2. **反复用 `git checkout` 恢复文件，把已完成的 DDL 一起回退了**，导致长时间在错误前提下排查
   （表现为 `Unknown column 'course_code'`）。教训：恢复文件前先确认该文件里有没有别处未提交的改动。

### 验证结果
- 后端 `mvn -o -B test -pl edu-api -am`：**Tests run: 63, Failures: 0, Errors: 0** ✅（连跑两次一致）
  - `GpaServiceTest` 10 项（绩点 13 例 + 通过判定 + GPA 加权 + 精度 + 学号校验）
  - `TrainingPlanAndCreditServiceTest` 12 项（方案定位/版本策略/学分/审核/无方案兜底）
- 新装库验证：`edujwxt.sql` + `seed_data.sql` 按 docker 顺序导入临时库**全部成功**，
  13 张表行数正确、`course_code` 无 NULL、`passed` 无 NULL ✅
- 临时库用完即删，未触碰真实库 ✅

### ⚠️ P1 尚未完成（原计划范围内）
REST 接口（`/gpa/my`、`/gpa/rank`、`/training-plan/**`）与权限登记、前端三个页面、
专业内绩点排名查询、Agent 新工具（原计划放 P5）。

---

## 2026-09-20（三）— 教务业务扩展设计定稿（文档，未实施代码）

用户提出把系统从**记录型**升级为**有规则的教学管理系统**，需求经逐条确认后，
形成设计文档 `docs/教务业务扩展设计.md`（578 行，8 节）。

### 新增文档内容
| 节 | 内容 |
|---|---|
| 一 | 背景、现状核对清单、**明确的非目标** |
| 二 | **核心业务规则**（绩点/补考/必修选修/时间冲突）——所有判断类功能的权威依据 |
| 三 | 数据库设计：11 张新表 + 5 处现有表改造，含字段/约束/索引/风险 |
| 四 | 分层设计、核心 Service、关键算法、REST 权限、**每张新表解锁哪些 Agent 工具** |
| 五 | P1~P5 分阶段计划（含依赖顺序与每阶段验收） |
| 六 | 测试设计（单元/集成/Agent 端到端/工具选择评测 + 验收清单） |
| 七 | 已知风险与应对 |
| 八 | **丢上下文后的接续方式**（环境、验证命令、已踩过的 9 个坑） |
| 附 | 8 条【假设】汇总，供用户复核 |

### 需求确认过程中的两次纠正（值得记录）

1. **我第一版设计的前提是错的**：我把"培养方案"当成既有数据去设计学分审核，
   但用户澄清**培养计划尚未建立**，需要新增表并由管理员维护。
   教训：**不要把"该由用户决定的业务模型"当成既定前提**。

2. **绩点公式我连续理解错两次**，最终由用户给出例子才定准：
   - 我先理解成分段映射（95→4.5、85→3.5…）
   - 再理解成 `floor(分数/10) - 5 + 小数部分`（漏了"余数要除 10"）
   - 用脚本实算用户给的 13 条例子，**只通过 4 条**，才发现错误
   - 正确公式：`绩点 = 分数/10 - 5`（分数 ≥ 60），实算 **11/11 全通过**
   教训：**规则类需求必须用例子逐条实算验证**，靠口头描述理解极易出错。
   该验证过程与错误记录已写入设计文档 §2.1，供实现时避坑。

### 需求范围（用户确认）
培养计划（按专业+年级分版）、必修/选修判定、考试安排、选课轮次开关、
选课安排（建议修读学期）、绩点与平均学分绩点、按 GPA 的专业内排名（学生仅见本人）、
教师排课与审批、选课时间冲突检测、补退选、补考、学籍状态字段（逻辑后续）。

### 用户明确否掉的项
单双周排课、排课优化、绩点排名缓存、重修流程（只做补考）、学籍状态逻辑。

### 验证结果
- 文档结构核对：8 节 + 9 子节完整 ✅
- 绩点公式：用脚本对用户 13 条例子实算，**11/11 通过**（含 60→1.0）✅
- 需求覆盖核对：用户提出的 19 条要求逐条在设计文档中有落点 ✅
- **本次仅文档，未改动任何代码**；后端测试仍为 41 项（未重跑，因无代码变更）

---

## 2026-09-20（二）— 修复「有哪些课程可以选？」不调用工具、反而追问的问题（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 6 个带可选筛选参数的工具，描述中补充「省略参数时返回全部」 | ✅ |
| 2 | 新增工具选择探针脚本，用数据对比描述改动前后的效果 | ✅ |
| 3 | 端到端验证脚本区分只读/危险操作，消除误报 | ✅ |

### 现象（用户实测发现）

用户问「有哪些课程可以选？」，AI 回答：
> 请提供想要搜索的课程名称…如果您不提供具体的课程名称，我将返回所有可选课程的信息。

即**模型反问用户**，而不是直接查询并列出课程。

### 排查过程（先证事实，再下结论）

1. **查审计表**：没有该次对话的记录 → 说明模型**根本没调用工具**，不是调用了出错。
2. **查后端日志**：无 `/ai/chat` 相关 SQL → 确认没有工具执行。
3. **复刻请求直接问 Ollama**：这一次却调用了工具 —— 但**凭空编造了参数** `courseName="leon"`（毫无来源）。
4. **跑 9 用例探针**：8/9 正确，只有「有哪些课程可以选？」这种问法失败。

结论：**不是稳定的功能缺陷，而是 7B 模型对「可选参数」处理不稳**，同一提问有三种行为：
反问用户 / 传空值 / 编造参数。而旧描述只说了"支持按名称搜索"，
**从未说明"不传会返回什么"**，给了模型猜测的空间。

### 修复

把「参数省略时的行为」明确写进工具描述与参数描述，覆盖全部 **6 个**带可选筛选参数的工具：
`get_course_list`、`list_users`、`list_students`、`list_teachers`、`list_courses`
（以及参数描述由"（可选，用于搜索）"改为"省略则返回全部"）。

`get_course_list` 的新描述还显式提示了触发说法：
> 查看可选课程列表。不传 courseName 时返回全部可选课程；用户说"有哪些课能选""列出所有可选课程"时，直接调用本工具且不要追问课程名称。

### 效果对比（同一个问题各跑 5 次，`.dsh/probe-one-case.cjs`）

| 描述版本 | 调用率 | 干净地请求全部 |
|---|---|---|
| 旧（改之前） | **3/5** | 3/5 |
| 新（改之后） | **5/5** | 5/5 |

旧描述下还出现了第三种失败：模型把工具调用当**文本**吐了出来
（`portlet {"name": "get_course_list", "arguments": {...}}`）。
新描述下 5 次全部返回干净的空参数 `{}`。

完整探针（8 用例）新描述下 **8/8 正确**，参数均干净，无编造。

### 端到端复验
- 只读查询「有哪些课程可以选？」：**5/5 通过**，AI 直接输出课程表格，
  且**未弹出确认卡片**（正好验证风险分级未判错）
- 危险操作「帮我选课程6」：**9/9 通过**，正常弹出确认卡片（工具=选课，参数 courseId=6）

### 顺带修正的工具问题
原验证脚本对只读查询也会断言"必须出现 confirm 事件"，属**误报**
（查课程是只读操作，本来就不该弹确认卡片）。
现改为 `--expect-confirm` 显式声明预期，只读场景反向断言「不应出现确认卡片」，
两种路径都能正确判定。

### 验证结果
- 后端测试：**41 项全绿**（改描述不影响任何测试）
- 工具选择探针：**8/8**
- 聚焦对比：旧 3/5 → 新 5/5
- 端到端：只读 5/5、危险操作 9/9

### 说明
这**不是**代码逻辑缺陷，而是**提示词/工具描述质量问题**。
7B 本地模型的指令遵循能力弱，描述上的模糊会被放大；
真实产品中此类问题通常靠"把工具描述写到不留歧义"来解决，
本次也顺带验证了「改描述 → 用探针量化效果」这一改进闭环。

---

## 2026-09-20 — 用本地 Ollama 完成真实 LLM 验证，并修复由此暴露的审计截断缺陷（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 用 Ollama + `qwen2.5:7b` 跑通真实 LLM 端到端闭环（此前一直标为验证缺口） | ✅ |
| 2 | 新增可复用验证脚本 `.dsh/verify-real-llm.cjs`（能自动响应流中的确认卡片） | ✅ |
| 3 | 修复由此暴露的缺陷：审计 `status` 列宽不足导致状态值被静默截断 | ✅ |
| 4 | 新增两条守护测试，其中一条回归测试若早存在则该缺陷不会发生 | ✅ |
| 5 | 测试 39 → **41 项**，连跑两次一致 | ✅ |

### 一、真实 LLM 验证（补上了长期存在的缺口）

本机装有 Ollama（`qwen2.5:7b` / `bge-m3` / `nomic-embed-text`）。
先验证其 OpenAI 兼容端点与 **tools capability**（`ollama show` 确认支持），可承接 Agent 工具调用。
**首次探测未触发工具调用，一度怀疑模型不支持；实际是请求写得不够明确**——加上明确的 system prompt 后
`finish_reason` 即为 `tool_calls`。这个误判值得记：把"我的测试请求有问题"错认成"能力不具备"。

启动方式（无需改代码）：
```
AI_BASE_URL=http://localhost:11434/v1  AI_MODEL=qwen2.5:7b  AI_API_KEY=ollama(占位)
```
⚠️ `OpenAiClient` 在 apiKey 为空时直接报错不发请求，故必须给占位值。

**闭环验证结果（`.dsh/verify-real-llm.cjs`，9/9 断言通过，23.4 秒）：**
- 学生 `2023001` 说"帮我选课，课程ID是5"
- 模型真实发起 `select_course(courseId=5)` 工具调用
- 服务端**未直接执行**，推送 `confirm` 事件挂起
- 脚本自动"点击确认"（另发 POST `/ai/confirm`，SSE 流保持打开）
- 服务端执行 → 审计落库 → 模型用自然语言汇报"你已经成功选上了课程ID为5的课程"
- 事件分布：`token 20 / status 1 / confirm 1 / confirm_result 1 / done 1`，无 error

**数据库侧铁证：**
- `course_selection` 真的多出 `(2023001, 课程5 高级英语)` —— 业务数据确实变更
- 审计表 127 → 128，新增记录 `status=SUCCESS`、`confirm_id` 与响应中的确认令牌一致、
  `args_json={"courseId":5}`、耗时 40ms
- 对比可见：此前测试写入的记录 user 是 `audit-` 前缀、`confirm_id` 为假值，这条是真跑出来的

### 二、由此暴露的缺陷：审计状态值被静默截断

真实模型提交越界评分（9999）触发参数校验路径后，日志出现：
```
审计字段超长已截断: column=status, maxLen=16, actualLen=17
```
**根因**：`status` 列为 `varchar(16)`，而 `INVALID_ARGUMENTS` 与 `DUPLICATE_SKIPPED` 都是 **17 字符**，
被写成 `INVALID_ARGUMENT` / `DUPLICATE_SKIPPE`。后果是**按 status 聚合时记录被分到错误分组**，
且"幂等命中"标记失真——审计数据在悄无声息地变错。

**为何 39 项单测没拦住**：测试只用了短状态值（SUCCESS/FAILED 等），
没有任何测试把常量长度与真实表结构做核对。这是"测试全绿但缺陷存在"的典型。

**修复过程本身也踩了一个坑，值得记录**：第一次只把数据库列扩到 `varchar(32)`，
**漏改了应用层的 `MAX_STATUS_LEN = 16`**，于是仍被应用层截断；
而当时新写的守护测试**只查了数据库列宽**，因此照样通过，直到再跑一次真实 LLM 才暴露。
→ 同一个限制存在两处（应用层常量 + 数据库列宽）时，**必须同时断言两侧**，
否则"只修一半"会被自己的测试放过。

最终修复：
- `status` 列 → `varchar(32)`，应用层 `MAX_STATUS_LEN` → 32
- 迁移脚本 `docs/sql/2026-09-20-fix-audit-status-width.sql`（可重复执行，含历史数据修正）
- 长度常量改为包级可见，供测试直接引用，避免测试里重复硬编码导致再次漂移

**新增守护测试（关键）**：
1. `auditLimitsMatchDatabaseColumns`：断言**应用层限制 ≤ 数据库列宽**，且所有状态/角色/风险常量装得下。
   （已实测有效：把常量临时改长后测试立即失败并报出具体长度）
2. `longStatusValuesArePersistedWithoutTruncation`：真实写入 `INVALID_ARGUMENTS` 并读回比对。
   **若此测试早存在，该缺陷不会发生。**

### 验证结果
- 后端 `mvn -o -B test -pl edu-api -am`：**Tests run: 41, Failures: 0, Errors: 0** ✅（连跑两次一致）
- 真实 LLM 闭环：**9/9 断言通过**，业务数据与审计记录双向核实 ✅
- 状态值完整性：`INVALID_ARGUMENTS` 17 字符已完整落库，无截断残留 ✅

### 仍未验证的部分
- `DUPLICATE_SKIPPED`（幂等命中路径）尚未被真实触发：重复提交同一确认令牌的正常路径
  已被 `ConfirmationGate` 与 Redis 删除挡住，该数据库层兜底分支需构造并发才能命中。
- 7B 模型的工具选择稳定性不足以作为准确率基准，M5 评测仍需云端模型或更大本地模型。

---

## 2026-09-19（收尾）— 新增「接续开发指南」并落盘工作计划（文档）

为方便下次继续本项目开发，在 `docs/Agent化升级计划.md` 顶部新增
**「接续开发指南（每次继续本项目先读这一节）」**，作为开工入口。

### 背景
本仓库配有 Hindsight 记忆库工具，但经 `hindsight_diagnose` 确认**不可用**：
`api_token_configured: false`，且配置文件 `C:\Users\53473\.hindsight\coding-agent.json` **根本不存在**，
调用返回 401。因此**唯一可靠的跨会话载体是本仓库自身**，工作计划一律落盘到仓库。

### 指南内容（5 节）
1. **快速定位**：想了解什么该看哪个文档；并标注 `docs/AI模块架构文档.md` **尚未更新到阶段 0 之后的状态**（下次改 AI 模块时应一并更新）
2. **环境启动**：Redis 硬依赖；离线环境 `mvn install` 不可用须走 `package` + `java -jar`；重建前须停后端（Windows 文件锁）
3. **当前验证方式**：三条命令 + 两条纪律（写真实存储的测试必须连跑两次；失败先查外部依赖是否在线）
4. **待用户确认事项**：`sendSse()` 既有缺陷是否现在修、`frontend/.vscode/` 是否提交、M2 是否一并拆 `views/ai/index.vue`、是否提供 `AI_API_KEY` 做真实对话验证
5. **下一步 M2 入口**：先做最小 Spring AI 接入验证（迁 1 个工具）再批量迁；多轮记忆是差距最大项；注意保留旧实现作回归对照

### 为什么单独写「接续开发指南」而不是只靠「进度追踪」
「进度追踪」回答"做到哪了"，但**不能回答"怎么开工"**：环境命令、验证方式、
等待用户决策的悬置项这些操作性信息此前散落在对话里，会随会话消失。
两者互补，都在同一文档内，避免下次再翻 git log 或开发记录倒推。

### 验证结果
- 文档章节结构核对：进度追踪 → 接续开发指南 → 一~八节，编号无缝、无标题丢失 ✅
- 本次仅文档改动，未触碰代码；测试仍为 39 项全绿（未重跑，因无代码变更）

---

## 2026-09-19（夜）— M1 阶段 0.2 / 0.6 / 0.8：阶段 0 收口（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 0.2 参数 JSON Schema 校验（自研轻量子集校验器） | ✅ |
| 2 | 0.6 幂等保护（以 confirmId 为幂等键，复用 `request_id` 唯一约束） | ✅ |
| 3 | 0.8 限流与预算（对话频次 + 单次工具调用上限，`maxIterations` 改配置） | ✅ |
| 4 | 测试从 18 项扩到 **39 项**，连跑两次结果一致 | ✅ |
| 5 | 端到端断言从 13 项扩到 **16 项**（新增限流边界验证） | ✅ |

**阶段 0（安全底座）至此全部完成。**

### 详细说明

**① 0.2 参数校验：为什么自研**
计划里写的是 `networknt/json-schema-validator`，但本机为**离线构建环境**，本地仓库中该库及同类库（everit / vertx）均不存在，无法引入新依赖。
因此改为自研轻量校验器 `JsonSchemaToolArgumentValidator`，**刻意只实现本项目工具实际用到的 Schema 子集**：
`object/string/integer/number/boolean/array` 类型 + `required` / `enum` / `minimum` / `maximum` / `minLength` / `maxLength` / `minItems` / `maxItems` / `items`。不冒充完整 JSON Schema 实现。
- 一次收集**全部**问题后回灌，让模型一轮改对，而不是挤牙膏式一次报一个
- 校验器自身异常时**放行并告警**：不能因校验实现的问题阻断工具执行
- 校验点在 HITL 确认**之前**——参数不合法就不该拿无效参数去打扰用户
- 新增审计状态 `INVALID_ARGUMENTS`，参数错误同样留痕

配套给真实工具补上约束：`evaluate_teacher.score` 加 `minimum:1, maximum:100`；`list_users.role` 加 `enum:[admin,teacher,student]`。
测试里专门有一条「现有 21 个工具的正常调用必须全部通过」，防止校验器过严把好请求挡在门外。

**② 0.6 幂等：把 `request_id` 用起来**
0.5 已建好 `request_id` 列与 `uk_ai_audit_request` 唯一约束，本轮接入：
- 以 **confirmId 作为幂等键**（同一确认令牌对应同一次写操作）
- 确认执行前先查 `findSuccessfulByRequestId`，命中则**直接复用既有结果、绝不重复写库**，并向用户说明"该操作此前已执行过"
- **只认 SUCCESS**：失败与拒绝不构成"已执行"，重试必须被允许
- 查询失败按"未执行过"处理（宁可重复执行一次，也不因审计查询故障挡掉正常操作），并告警

本轮幂等的**范围**需要说清：只覆盖经人工确认的写操作（那正是重复提交风险最高处）。
未对直连写工具（如 `drop_course`）按内容做幂等——「重复选同一门课」本身是合法操作，按内容去重会误伤；且业务层已有唯一约束兜底。这是有意的取舍，不是遗漏。

**③ 0.8 限流与预算**
- `AgentRateLimiter`：Redis 固定窗口计数（`INCR` + 首次设 TTL），按用户维度
  - 对话频次：默认 10 次/分钟
  - 单次对话工具调用总量：默认 20 次，防止模型在同一轮内狂调工具
- `maxIterations` 从硬编码 10 改为配置项 `ai.limits.max-iterations`，默认降到 **5**
- Redis 不可用时**放行并告警**（限流是防滥用手段，不应让功能整体不可用）
- 支持环境变量覆盖：`AI_MAX_CHATS_PER_MINUTE` / `AI_MAX_ITERATIONS` / `AI_MAX_TOOL_CALLS_PER_CHAT`

### 验证结果
- 后端 `mvn -o -B test -pl edu-api -am`：**Tests run: 39, Failures: 0, Errors: 0** ✅
  - `AiAuditServiceTest` 9、`JsonSchemaToolArgumentValidatorTest` 13、`AgentRateLimiterTest` 5（真实 Redis）、`ConfirmationGateTest` 5、`ToolRegistryTest` 7
- 端到端 `.dsh/verify-m1.ps1`：**PASS=16, FAIL=0** ✅
  - 限流边界精确命中：连发 12 次 `/ai/chat`，**10 通过 / 2 拒绝**，与配置的 10 次/分钟一致
- 连续两次运行结果一致（不重犯上一轮"只跑一次就认为稳定"的错）

### ⚠️ 排查记录：一次「以为是代码 bug，其实是环境」
`AgentRateLimiterTest` 首次运行 4/5 失败，表现为「计数恒为 0、全部放行」。
最初怀疑限流逻辑有误，**实际根因是本机 Redis 已停止运行**（`Connection refused`），
而限流器按设计"Redis 不可用时放行"，于是症状伪装成了逻辑错误。
教训：测试失败先确认外部依赖是否在线（该连接失败在日志里本就有告警，应先看日志再改代码）。
另需注意：本项目 Redis 是**硬依赖**——`LoginInterceptor` 校验 token 也走 Redis，Redis 不可用时登录本身就会失败。

### ⚠️ 验证缺口（与上一轮相同，仍未消除）
本机无 `AI_API_KEY`，「agent 循环 → 工具执行 → 审计落库」的完整链路**仍未经真实 LLM 端到端验证**。
本轮证据是：单元/集成测试（真实 MySQL、真实 Redis）+ 真实 HTTP 安全断言。
其中**参数校验与限流**两条链路不需要模型即可验证，已充分覆盖；幂等与审计的"真实模型触发"部分待配置 Key 后补验。

### 阶段 0 总览（全部完成）
| 任务 | 状态 |
|---|---|
| 0.1 工具白名单双校验 | ✅ |
| 0.2 参数 JSON Schema 校验 | ✅ |
| 0.3 危险操作 HITL 确认 | ✅ |
| 0.4 确认令牌服务端持有 | ✅ |
| 0.5 审计表与四类状态落库 | ✅ |
| 0.6 幂等保护 | ✅ |
| 0.7 错误脱敏 | ✅ |
| 0.8 限流与预算 | ✅ |

---

## 2026-09-19（晚）— M1 阶段 0.5：工具调用审计落库（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 新增 `ai_tool_audit` 表（迁移脚本 + 同步 `edujwxt.sql`） | ✅ |
| 2 | 实体 `AiToolAudit` + `AiToolAuditMapper`（注解 SQL） | ✅ |
| 3 | `AiAuditService`：JSON 序列化、截断防御、失败不外抛 | ✅ |
| 4 | 接入 `AiChatService` 四条路径（成功/失败/越权/用户拒绝） | ✅ |
| 5 | 修正 `awaitConfirmation` 返回值以携带 `confirmId`（原实现必然写出空值） | ✅ |
| 6 | 测试 6 项，全部在真实 MySQL 上验证落库 | ✅ |
| 7 | 排除 Mockito 依赖（JDK 21 下无法初始化，阻断全部 `@SpringBootTest`） | ✅ |

### 详细说明

**① 表设计要点**
- 状态取值：`SUCCESS` / `FAILED` / `DENIED`（越权或未知工具）/ `REJECTED_BY_USER`（用户取消或确认超时）
- `confirm_id`：人工确认过的写操作可回溯到具体的那一次确认
- `request_id` + 唯一约束：现已建好，为阶段 0.6 的幂等保护预留（当前写 NULL，MySQL 唯一索引允许多个 NULL，不影响现有写入）
- 索引按 `user_id` / `tool_name` / `status` 分别配 `created_at`，支撑阶段 4 的聚合查询
- 迁移脚本用 `CREATE TABLE IF NOT EXISTS`，**可重复执行**（已实测重复执行 exit=0）
- `AiToolAuditMapper` 刻意只有 insert / 查询，**没有 update 与 delete**：审计是不可变事实

**② 设计取舍**
- **审计失败绝不外抛**：用户不该因为日志表写不进去而无法选课。但失败必须留 `ERROR` 级日志——「审计静默失效」比「没有审计」更危险。该不变量有专门测试覆盖。
- **越权调用的风险等级记 `UNKNOWN`**：不返回该工具真实的 `risk_level`。调用者本就无权使用它，泄露其存在性与等级属不必要的权限信息外泄。
- **定长列截断防御**：`user_id`/`role`/`tool_name`/`risk_level`/`status`/`confirm_id` 超出列宽时截断并告警，而不是让 MySQL 拒绝整条 INSERT。

**③ 修掉一个「写了但不可能生效」的实现**
`awaitConfirmation` 原返回 `String`，且内部在 `awaitDecision` 之后立即把 `awaitingConfirmId[0] = null`。
因此拒绝路径若去读 `awaitingConfirmId[0]` 记录审计，`confirm_id` **永远是空**。
改为返回 `ConfirmationOutcome(approved, confirmId, result)` 显式带出令牌——审计需要它，不能依赖已被清空的挂起状态。

**④ 测试过程中踩到的三个坑（都已修正，值得记录）**
1. **`Data too long for column 'user_id'`**：测试用 `"audit-test-" + 完整 UUID`（47 字符）超出 `varchar(32)`，INSERT 被 MySQL 拒绝。而「审计失败不外抛」的设计把它伪装成了「查不到数据」这一误导性症状——排查花了几轮。教训：吞异常的防御性设计必须配合足够醒目的告警，否则会掩盖真实故障。
2. **`@SpringBootTest` 默认回滚**：Spring Boot 给每个测试方法套一个回滚事务，审计写入在方法内即被回滚（计数不增、查询为空）。已实测确认后加 `@Transactional(propagation = NOT_SUPPORTED)` 关闭，审计测试才有意义。
3. **测试间数据污染**：审计表只追加、不回滚，共用同一 `user_id` 会让 `listByUser` 读到历史运行累积的记录（期望 1 条实际 3 条）。改为每个测试生成独立 `user_id`。
4. **「测试通过」不等于「测试稳定」**：修好上面三点后单次 18/18 全绿，但**第二次运行**时 `oversizedIdentifierIsTruncatedInsteadOfRejectingTheWholeRow` 失败（期望 1 条实际 2 条）——该用例输入是固定的 `"u".repeat(100)`，截断后每次都得到同一个 32 字符 `user_id`，按它查回必然累积。
   改为**直接断言服务端清洗后的写入值**（这正是被测行为，且与历史数据无关），并为此给 `AiAuditService` 加了一个把写入实体回传的包级重载（仅供测试观察清洗结果）。
   教训：只要测试会写真实持久化存储，就必须验证**连续两次运行的结果一致**，而不是只看一次绿。

**⑤ Mockito 依赖被排除**
`spring-boot-starter-test` 3.5.16 在离线仓库中解析到的 mockito/byte-buddy 组合在 JDK 21 上无法初始化 `MockMaker`。且 Spring Boot 测试支持会在**上下文启动时主动探测 Mockito**，导致全部 `@SpringBootTest` 用例失败（与是否真的用 mock 无关）。已在 `edu-api/pom.xml` 排除 `mockito-core` 与 `mockito-junit-jupiter`；需要桩时手写，本项目测试不依赖 Mockito。

### 验证结果
- 后端 `mvn -o -B test -pl edu-api -am`：**Tests run: 18, Failures: 0, Errors: 0** ✅
  - `AiAuditServiceTest` 6 项（真实 MySQL）
  - `ConfirmationGateTest` 5 项、`ToolRegistryTest` 7 项
- 真实库落库确认：`ai_tool_audit` 共 15 行，四种状态齐全
  - `SUCCESS 9` / `FAILED 2` / `DENIED 2` / `REJECTED_BY_USER 2`
  - `MAX(CHAR_LENGTH(user_id)) = 32`（截断防御生效）
  - `MAX(CHAR_LENGTH(result_json)) = 60014`（超长截断生效，带 `...[truncated]` 标记）
  - `confirm_id` 非空记录存在（approved/rejected 路径均写入）
  - 下划线列名 → 驼峰属性映射正确（逐字段读回校验）
- 端到端回归 `.dsh/verify-m1.ps1`：**PASS=13, FAIL=0**（审计接入未破坏运行时）

### ⚠️ 验证缺口（诚实记录）
**未经真实 LLM 驱动的完整链路验证。** 本机未配置 `AI_API_KEY`，无法让模型真正发起工具调用，因此
「agent 循环 → 工具执行 → 审计落库」这一整条链路**没有端到端跑通**。当前证据是：
- `AiAuditService` 在真实库上四类状态均可写入并读回（集成测试）
- `AiChatService` 四条调用点的代码已接入（代码审查）
待配置 API Key 后应补一次真实对话验证。

### ⚠️ 顺带发现的既有缺陷（未修，非本次引入）
`AiChatService.sendSse()` 只捕获 `IOException`，而 `SseEmitter` 在已完成后调用 `send()` 抛的是
`IllegalStateException("ResponseBodyEmitter has already completed")`，该异常会冒泡到
`processChat` 外层被记为 `AI chat processing error`，产生**误导性的错误日志**。
触发条件：客户端在 agent 处理中途断开（实测在验证脚本读取 SSE 后连接关闭时复现）。
已核对 `git diff`：`sendSse` 及其异常处理与 HEAD 完全一致，**属既有问题**。
修法很简单（catch 一并捕获 `IllegalStateException`），但未在本轮改动范围，留待确认。

---

## 2026-09-19（下）— M1 安全底座落地 + 真实链路端到端验证（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 停止跟踪 `frontend/node_modules`（已跟踪文件 12143 → 170） | ✅ |
| 2 | M1 后端（工具白名单 + 危险操作 HITL 确认）单元测试 12 项全绿 | ✅ |
| 3 | 真实环境端到端验证：13 项断言全通过（MySQL + Redis + 真实 HTTP） | ✅ |
| 4 | 新增本地 Redis 开发配置与验证脚本，纳入版本管理 | ✅ |
| 5 | 更正一处我自己作出的错误缺陷判定（SSE charset） | ✅ |

### 详细说明

**① 仓库清理：`frontend/node_modules` 移出版本库**
- 新增仓库根 `.gitignore`（此前仓库根**没有** `.gitignore`）：`node_modules/`、`dist/`、`target/`、`.idea/`、日志、测试产物等
- 执行 `git rm -r --cached frontend/node_modules`（磁盘目录保留，81 个包不受影响）
- 提交 `b42d11d`。已跟踪文件 **12143 → 170**（业务代码 169 + `.gitignore`）
- 验证：`git check-ignore` 确认忽略生效；`git status` 中 **0 条**未跟踪的 node_modules（11,974 条均为已暂存删除）
- 根因：`.gitignore` 位于 `frontend/edu-system-client/`，而依赖装在 **`frontend/`** 根下，位置不匹配且已跟踪文件不受 `.gitignore` 约束

**② M1 真实链路端到端验证（`.dsh/verify-m1.ps1`，13 项断言全通过）**

环境：Windows + MySQL 8.4.7（`edujwxt` 库，10 张表、种子数据齐全、安全迁移唯一约束已生效）+ Redis 5.0.14.1（本地，`.dsh/redis-dev.conf`）+ fat jar 运行后端。

覆盖项：
- 鉴权边界：无 token 访问 `/ai/config` → 401；无 token 调 `/ai/confirm` → 401
- 登录链路：`admin01`（admin）与 `2023001`（student）均能取到有效 token
- 角色鉴权：学生访问 `/user` → 403
- `/ai/config`：AI 未配置时 `configured=false`，且**响应不暴露 apiKey**（仅 `configured/model/baseUrl`）
- **`/ai/confirm` 三条防线**：缺 `confirmId` → 400；伪造 `confirmId`（服务端无暂存记录）→ 410；**跨用户确认 → 410**（admin 无法确认归属于 student 2023001 的待确认操作，通过向 Redis 注入他人 `ai:pending:` 记录实测）
- AI 未配置时的降级：`/ai/chat` 返回 SSE 且带明确错误事件，**不静默失败**

**③ 一处自我更正：SSE 的 Content-Type 没有 charset，这不是缺陷**
我最初把「`text/event-stream` 未声明 `charset=utf-8`」判为真实缺陷，理由是实测同一响应用 UTF-8 与 Latin-1 解码结果不同（确实不同）。
**该判定是错的**：SSE 规范规定 `text/event-stream` 只有 UTF-8 一种有效编码、且无法声明其他编码，因此不带 charset 参数是**规范正确**行为，浏览器与遵循规范的客户端一律按 UTF-8 处理（本项目前端 `TextDecoder` 亦默认 UTF-8）。
曾尝试给 `SseEmitter` 传 `MediaType.TEXT_EVENT_STREAM`，经 `javap` 确认 Spring 6.2.8 的构造器只有 `()` 与 `(Long)`，该重载不存在，遂回退。脚本中改为输出 `[NOTE]` 而非断言。

**④ 环境踩坑（对后续开发有复用价值）**
- **Windows PowerShell 5.1 把 `.ps1` 按 ANSI 读取**：脚本内含中文会破坏语法解析（报"意外的标记"）。故验证脚本刻意写成纯 ASCII，对中文响应的断言改用 UTF-8 字节解码 + Unicode 码点拼接（`[char]0x5BC6`）。
- `Invoke-WebRequest` 在响应头无 charset 时按 Latin-1 解码 body，直接字符串匹配中文会得到乱码与假阴性 → 应读 `RawContentStream` 自行 UTF-8 解码。
- `mvn install` 在离线模式缺 `maven-install-plugin` 依赖 → 改用 `mvn package` 产出 fat jar，以 `java -jar` 启动，绕开 `install`
- `mvn spring-boot:run -pl edu-api -am` 会把 `run` 目标作用到父 pom（无主类）而失败 → 应先构建，再对目标模块运行
- 重建 fat jar 前必须先停止运行中的后端（Windows 文件锁导致 `repackage` 无法 rename）

### 验证结果
- 后端 `mvn -o -B test -pl edu-api -am`：**Tests run: 12, Failures: 0, Errors: 0** ✅
- 后端 `mvn -o -B package -DskipTests`：BUILD SUCCESS ✅
- 端到端 `.dsh/verify-m1.ps1`：**PASS=13, FAIL=0** ✅

### ⚠️ 尚未完成（M1 剩余项，取自 `docs/Agent化升级计划.md` 阶段 0）
0.2 参数 JSON Schema 校验、0.5 `ai_tool_audit` 审计表、0.6 幂等保护、0.8 限流与预算。
另：本轮验证依赖本机 MySQL/Redis；Redis 需手动启动（`.dsh/redis-dev.conf`）。

---

## 2026-09-19 — 前端技能治理 + 全量前端代码审核（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 建立前端技能路由与仲裁规范（`docs/技能使用规范.md` + 项目级技能 `.dsh/skills/edu-frontend-rules.md`） | ✅ |
| 2 | 用 `fe-ui-ts-linter` 审核，先修 `views/ai/index.vue` 后扩展到全部 22 个 `.vue` | ✅ |
| 3 | SSE 事件协议类型化（`AgentEvent` 判别联合，替代 `data: any`） | ✅ |
| 4 | 全项目 `any` 清零（10 处 → 0），`catch` 一律 `unknown` | ✅ |
| 5 | 项目级 linter 配置 `.dsh/feui-lint.json`（含规则取舍理由） | ✅ |
| 6 | 合并 7 个表单组件的重复接口定义，统一到 `types/models.ts` | ✅ |
| 7 | 修正 3 处 Element Plus 重复弹窗 + 3 处未使用导入 | ✅ |

### 详细说明

**① 技能治理的背景与结论**
`design-taste-frontend`（`~\.agents\skills\`）与 `vue-best-practices`（Vue 官方 `vuejs-ai` 仓库 v18.0.0）存在**方向性不对称**：后者遇非 Vue 项目会降级分派，前者没有任何降级分支，其 SKILL.md §2 直接假定 "React or Next.js + Tailwind 90%"。而它的触发条件是「任何前端/UI 工作」，在本项目每次改前端都会生效。

处置分三层，本次落地前两层：
- **第一层 职责切分**：代码维度（组件拆分、数据流、响应式、类型契约、composable）听 `vue-best-practices`；视觉维度只从 `design-taste-frontend` 裁取可迁移部分（§5 硬件加速守则、§3 Rule 5 交互四态、§3 Rule 6 表单模式等），并明确排除其 §2 全部栈约束、§4/§9 的 Framer Motion 全套、§3 Rule 2 的「紫色蓝色禁用」（与主色 `#165DFF` 冲突）。
- **第二层 硬仲裁**：项目现状优先 → 代码问题听 Vue 规范 → 视觉问题降级为建议且不改技术栈 → 正交则同采 → 无法裁决时显式交用户。
- 第三层（作用域隔离）未执行，按用户决定保留全局安装。

规则落在**项目级技能**中而非仅写文档，因此随仓库版本控制、跨会话自动加载（`.dsh/` 未被 git 忽略，已用 `git check-ignore` 验证）。

**② 审核发现的真实缺陷（已修）**

*第一轮（`views/ai/index.vue`）：*
- `let currentAssistantMsg = ref<ChatMsg | null>(null)` → `const`（从未整体重新赋值）
- `handleSseEvent(data: any)` 收到的是后端自定义 JSON，改为 `AgentEvent` 判别联合类型，与 `AiChatService` 发送的事件一一对应（`token`/`status`/`error`/`confirm`/`confirm_result`/`done`）
- 2 处 `catch (err: any)` → `catch (err: unknown)` + `errorMessage()` 类型收窄
- `const res: any = await request.get('/api/ai/config')` → `AiConfigResp` 接口

*第二轮（扩展到全部 22 个 `.vue` 文件）：*
- **`any` 全项目清零（10 处 → 0）**。除上述外还包括：`evaluate/index.vue`、`course/index.vue` 的 `catch (e: any)`；`user/index.vue` 的 `handleDelete(id: any)`、`handleStatusChange(row: { id: any; status: any })`。
- **7 个表单组件的重复接口定义合并**。`CollegeForm`/`CourseForm`/`ClazzForm`/`MajorForm`/`ScoreForm` 各自声明了与 `types/models.ts` 重复的 `College`/`Course`/`Clazz`/`Major`/`Score`，且字段类型不一致（本地 `collegeId`/`majorId` 为 `number | string`，共享类型为 `number`）。现统一由 `types/models.ts` 提供，并为各 `form` ref 标注类型。
  - 由此暴露出共享类型与表单真实需求不符：表单 `el-select` 未选择时值为 `''`，故 `Course.collegeId`、`Major.collegeId`、`Clazz.majorId` 统一改为 `number | string`；`Score.id` / `Score.totalScore` 改为必填（表单以 `0` 表示"尚无记录"）。这些是**类型表达真实契约**的修正，不是放宽。
- **5 个表单的 `setData(row: any)` → 精确类型**。
- **3 处 Element Plus 重复弹窗**：`Login.vue`、`evaluate/index.vue`、`course/index.vue` 的 catch 各自再弹一次错误提示，而 `request.ts` 拦截器已统一弹窗（业务 `code!=200` / 401 / 403 / 网络错误），属**双弹窗**。已改为不重复提示。`Login.vue` 处另使用了 `err.response?.data`（错取 `data`，实际消息在 `msg`）。
- **3 处未使用导入**：`clazz/index.vue` 的 `ElFormItem`（`main.ts` 已 `app.use(ElementPlus)` 全局注册，无需单独导入）、`course/index.vue` 与 `evaluate/index.vue` 的 `computed`。删除前逐一用 `Select-String` 独立核实出现次数，未直接采信 linter 的文本匹配结论。

**③ 工具自身的两个 bug（实测才发现，值得记录）**
本次审核用的自检脚本 `.dsh/lint-ai-view.cjs` 里新增的 `vue/prefer-const-ref` 规则**连续错两次**：
1. 先误判为「`let` 与变量名之间的多空格对齐」问题——猜测错误；
2. 真因是**泛型**：实际代码为 `ref<ChatMsg | null>(null)`，而模式写的 `ref\(` 无法匹配 `ref<...>(`。

教训：**没有任何技能能发现你自己的规则写错了，只有实测能。** 该规则若未经实测，会给出「0 命中」的假阴性结论。

另记录 `fe-ui-ts-linter` 内置规则集的三个局限（均已在配置中处置）：
- `ui/require-key` 按 React 的 `items.map(item => <li>)` 模式实现，只认 `.map()`，不区分「数组映射」与「列表渲染」，也完全不识别 Vue 的 `v-for` → **对 Vue 项目属误报源**，已关闭，改用本项目补充规则 `vue/v-for-require-key`。
- `ts/no-explicit-any` 用纯正则匹配，**会命中注释文本**（曾把一句说明注释判成 `any`）→ 通过改写注释规避。
- `a11y/label-for` 只认 React 的 `htmlFor`，Vue 用 `for`（其判断里也含 `for\s*=`，故未误报）。

**④ 模板合规性核实**
3 处 `v-for` 全部带 `:key`（含新增的确认卡片 `:key="item.key"`）；无内联 `style`；配色沿用主色 `#165DFF`，未受「LILA BAN」影响。

### 验证结果
- 前端 `npm run type-check`（vue-tsc --build）✅
- 前端 `npm run build-only` ✅（产物 built，仅既有 chunk 体积告警）
- `fe-ui-ts-linter` 复审：**0 error / 0 warn / 0 info** ✅

### ⚠️ 本次审计发现、尚未处理的仓库问题
**`frontend/node_modules` 被 git 跟踪，占已跟踪文件的 98.6%**（11,974 / 12,143；业务代码仅 169 个文件）。
根因：`.gitignore` 位于 `frontend/edu-system-client/`，而 `node_modules` 实际安装在 **`frontend/`** 根下，两者位置不匹配，且已被跟踪的文件不受 `.gitignore` 约束。
危害：`git status`/clone/子 agent 的 grep 与 glob 被大量第三方文件淹没（本项目早前的 glob 结果确实混入 1400+ 条 node_modules 路径），是 Agent 化开发的实际效率损耗。

### ⚠️ 待办（下一步候选）
1. **将 `frontend/node_modules` 移出版本库**：在仓库根新增 `.gitignore` 忽略 `node_modules/`，执行 `git rm -r --cached frontend/node_modules` 后提交（该目录仍保留在磁盘，不影响本地开发）。
2. **拆分 `views/ai/index.vue`**：文件已达 752 行。按 `vue-best-practices` 的客观拆分触发条件（同时拥有编排/状态与多个 UI 区块、3+ 独立 UI 段、CRUD 类特性应至少拆为容器/表单/列表/页脚），它已构成其明确定义的「mega component」反模式，应在 M2 事件协议阶段一并拆分。

---

## 2026-08-13/14 — 第三批：工程化改造（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 后端 N+1 查询修复（TeacherMapper JOIN 学院、CourseMapper.selectByIds IN 查询、AI 工具同步） | ✅ |
| 2 | 根 pom `<mysql.veison>` 拼写修复；edu-common/edu-pojo Java 23 → 21 统一 | ✅ |
| 3 | Spring Boot 3.3.4（已 EOL）→ **3.5.16**；mybatis 3.0.3→3.0.5；mysql 驱动 9.3→9.7；lombok 1.18.46；**jjwt 0.11.5→0.13**（JwtUtil 改写为新 API） | ✅ |
| 4 | 清理 Layout.vue / user/index.vue 的 debug console.log | ✅ |
| 5 | 15 个表格加 v-loading + empty-text="暂无数据"，各页面加载函数补 loading 开关 | ✅ |
| 6 | 8 个 CRUD index.vue 补 `lang="ts"`：新建 `src/types/models.ts` 实体类型，全部 ref/函数参数类型化，strict 检查通过后**移除 env.d.ts 的 `declare module '*.vue'` 兜底声明** | ✅ |
| 7 | Docker 部署：前后端 Dockerfile、nginx.conf（含 SSE 反代配置）、docker-compose.yml（mysql+redis+backend+frontend，含健康检查） | ✅ |
| 8 | README 重写：技术栈、本地开发、测试账号、Docker 部署、安全说明 | ✅ |

### 详细说明
- **N+1**：`TeacherMapper.list/listAll` 改为 JOIN college 直接带出 collegeName（TeacherController 删除循环查询，同时消除了"注入实现类"问题）；`CourseMapper.selectByIds` 批量查询；AI 学生工具 get_my_courses 同步改为 IN 查询
- **依赖升级**：Boot 3.5.16 编译 + package 打包通过；jjwt 0.13 为破坏性升级（`parserBuilder()` → `parser().verifyWith()`、`setClaims` → `claims()`），JwtUtil 已适配，登录/拦截器逻辑不变
- **类型安全**：8 个核心页面进入 strict 检查范围，实体类型集中在 `src/types/models.ts`（与后端 com.duyell 实体对应）；`formRef` 调用 setData/reset 因 `ref()` 无参仍为 any（后续可补 InstanceType 类型）
- **部署**：docker compose 启动后前端 80 端口 Nginx 托管静态资源并反代 /api（剥离前缀），SSE 关缓冲；MySQL 首次启动自动执行 edujwxt.sql + seed_data.sql

### 验证结果
- 后端 `mvn compile` ✅、`mvn package` ✅（fat jar：edu-api-0.0.1-SNAPSHOT.jar）
- 前端 `vue-tsc --noEmit` ✅、`npm run build` ✅
- Docker 镜像构建 ⚠️ 未验证：本机 Docker Desktop 未启动（配置已就绪，启动后执行 `docker compose up -d --build` 即可）

---

## 2026-08-13（下午）— 第二批：正确性缺陷修复（完成）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | SysUserServiceImpl.update 重复执行 update + NPE 修复 | ✅ |
| 2 | TeacherServiceImpl.add() 补 @Transactional；StudentServiceImpl 注入 PasswordEncoder bean | ✅ |
| 3 | 全局异常处理器：新增 BusinessException，业务/系统异常分离（不泄露 SQL 细节、不打业务堆栈） | ✅ |
| 4 | CourseMapper.xml 容量筛选条件写反修复（`<=` → `>=`） | ✅ |
| 5 | 成绩 totalScore 服务端统一计算（平时 0.4 + 考试 0.6，不信任前端；单传一项自动合并原值） | ✅ |
| 6 | 前端 request.ts 拦截器处理业务 code!=200（统一 ElMessage + reject）；403 单独提示 | ✅ |
| 7 | 8 个 getList 补 try/catch；12 处空 catch 补 console.error；saveAllScores 失败计数提示 | ✅ |
| 8 | 前端路由角色守卫：meta.roles + beforeEach 校验（学生无法直达 /user 等管理页） | ✅ |
| 9 | 教师评价平均分：新增后端聚合接口 /evaluate/teacher/avg（修复只算当前页 10 条的问题） | ✅ |
| 10 | 顺手：删除 .claude/settings.local.json 中已作废的旧 API Key 行 | ✅ |

### 详细说明
- **BusinessException**（`edu-common/utils`）：业务错误提示用户不打印堆栈；系统异常返回"系统繁忙"且记完整日志。替换了 LoginServiceImpl（5 处）、SysUserServiceImpl（5 处）、CourseSelectionServiceImpl（4 处）的业务异常
- **前端拦截器**：业务错误统一弹窗提示 + reject，调用方 catch 里不再重复弹窗（saveScore 的双重弹窗已消除）
- **路由守卫**：admin 专属页 = /user /teacher /student /clazz /college /major；/evaluate = teacher+student；其余三种角色共用；无权限跳回 /index
- **注意**：GlobalExceptionHandler 现在对系统异常返回"系统繁忙，请稍后重试"而非原始 message，排查问题需看后端日志

### 验证结果
- 后端 `mvn compile` ✅
- 前端 `npm run type-check` ✅、`npm run build-only` ✅

---

## 2026-08-13 — 全面审查 + 第一批安全修复（完成）

### 背景
- 基于 2026-05-15 首次审查（30 项问题清单）做了一次全面复审，并发现新的安全问题。
- 复审结论摘要：
  - 后端 19 项旧问题：8 修复、4 部分修复、7 未修
  - 前端 10 项旧问题：3 修复、1 部分修复、6 未修
  - 新发现安全红线：后端零角色鉴权、JWT 密钥硬编码、AI 教师工具无归属校验、AI 聊天 XSS、选课无并发控制、数据库无唯一约束

### 本次修改清单（第一批：安全修复）

| # | 修改项 | 状态 |
|---|--------|------|
| 1 | 恢复 2026-05-15 问题清单存档，建立本开发日志 | ✅ |
| 2 | 后端角色鉴权（拦截器按角色拦截管理接口） | ✅ |
| 3 | JWT 密钥环境变量化 + 拦截器比对 token 值 + TTL 统一 | ✅ |
| 4 | AI 教师工具课程归属校验（顺带修复只传平时成绩时考试成绩被清零的 bug） | ✅ |
| 5 | 前端 AI 聊天 DOMPurify XSS 消毒 | ✅ |
| 6 | 选课查重/容量校验/事务 + 数据库唯一约束（迁移 SQL） | ✅ |
| 7 | 构建验证（后端 mvn compile + 前端 type-check/build 通过） | ✅ |

### 详细改动

**① 后端角色鉴权（`edu-common/.../interceptor/LoginInterceptor.java` 重写）**
- 新增权限规则表：`/user /teacher /student /clazz /college /major` 仅 admin；`/course` 写操作仅 admin；`/score` 仅 admin+teacher；`/course-selection`、`/evaluate` 仅学生（admin 全程放行）；`/course/my`、`/course/{id}/students` 为 admin+teacher
- 未命中规则的接口维持"仅需登录"（/home、/ai 等）
- 拦截失败返回 403 JSON；预检 OPTIONS 放行
- **数据归属校验（IDOR 修复）**：`ScoreController` 教师查/录/改/删成绩必须指定课程且课程属于自己（`CourseService.isCourseOfTeacher` 新增）；`CourseController` 教师只能看自己课程的选课名单

**② JWT 加固**
- `JwtUtil.java`：密钥改为配置注入 `jwt.secret: ${JWT_SECRET:...}`（生产必须设环境变量），过期时间可配 `jwt.expiration-minutes`
- `LoginInterceptor`：Redis 中的 token 与请求头比对，**重新登录后旧 token 立即失效**
- `LoginServiceImpl`：Redis TTL 从 7 天改为与 JWT 有效期一致（30 分钟），不再残留无用数据

**③ AI 工具归属校验（`TeacherToolRegistrar.java`）**
- `get_course_students`/`enter_score`/`update_score` 均校验课程属于当前教师
- `update_score` 改为先查现有记录、合并字段后重算总分（修复原来只传平时成绩时考试成绩被清零的 bug）
- 学生 `select_course` 工具改为复用新的选课服务（含事务/锁）

**④ 前端 XSS（`views/ai/index.vue`）**
- 新增依赖 `dompurify`，AI 回复经 marked 渲染后 `DOMPurify.sanitize` 再 `v-html`

**⑤ 选课并发控制**
- 新增 `CourseSelectionService`（接口+实现）：选课事务内 `SELECT ... FOR UPDATE` 锁课程行 → 查重 → 容量校验 → 插入；唯一约束兜底重复插入
- `CourseSelectionController` 重构为调用 Service（同时修复了"Controller 直连 Mapper"的架构问题）
- `CourseMapper.selectCourseByIdForUpdate`、`CourseSelectionMapper.select/countByCourseId` 新增

**⑥ 数据库约束（`docs/sql/2026-08-13-security-migration.sql`，需手动执行一次）**
- 清理重复数据 + `course_selection`/`score`/`teacher_evaluation` 三张表加 (course_id, student_id) 唯一约束
- 高频查询列补索引：course_selection.student_id、score.student_id、course.teacher_id、clazz.major_id、major.college_id、teacher_evaluation.teacher_id
- `edujwxt.sql` 已同步更新（新装库自带约束与索引）

**⑦ 前端依赖与类型修复（顺手修复）**
- `package.json` 补声明 element-plus/@element-plus/icons-vue/axios（此前靠父目录提升，单独安装会失败）
- `pnpm-workspace.yaml` 移除 `vue: 'beta'` 覆盖（会导致解析出 vue 3.6.0-beta 预发布版）
- `StudentForm`/`TeacherForm` 的 defineEmits 补 `'close'`（声明与实际使用不符）
- `env.d.ts` 添加 `declare module '*.vue'` 兜底声明（8 个 index.vue 尚缺 lang="ts"，待补全后移除）
- 依赖管理器确认：本项目实际使用 **npm**（package-lock.json），勿再用 pnpm install（会重新生成 pnpm-lock.yaml 且受覆盖影响）

### ⚠️ 待用户手动处理
1. **DeepSeek API Key 轮换**：`.claude/settings.local.json` 第 17 行有真实 key 明文，请到 DeepSeek 平台作废并换新，然后通过环境变量 `AI_API_KEY` 注入（后端已支持）
2. **执行数据库迁移**：`docs/sql/2026-08-13-security-migration.sql`（在 edujwxt 库执行一次）
3. **生产部署时设置 `JWT_SECRET` 环境变量**（默认开发密钥仅限本地使用）

### 验证结果
- 后端：`mvn compile` ✅
- 前端：`npm run type-check` ✅、`npm run build-only` ✅

---

## 历史记录

### 2026-08-13 之前（git 提交摘要）
- `93dfc03` feat: 添加AI对话助手、全局异常处理、种子数据，修复多项功能
- `ff06334` feat: 完成学生和教师角色的选课退课、成绩查询、教师评价功能
- `453fe80` feat: 完成管理员页面下的学院、专业、班级、课程页面，以及成绩管理的大部分
- （更早提交见 `git log`）
